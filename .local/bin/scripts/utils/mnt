#!/bin/sh
# mount disk
set -- $(lsblk -rno name,mountpoint,fstype | grep -i '  [a-rt-z]' | menu -0 -p 'Mount')
[ "$1" ] || exit
mkdir -p "$HOME/mnt/$1"
sudo mount "/dev/$1" "$HOME/mnt/$1"
notify-send "Mounted $1"
