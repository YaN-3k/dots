#!/usr/bin/env sh
chosen=$(grep -v "#" ~/.local/share/dots/kaomoji | ${DMENU:-dmenu})
[ ! "$chosen" ] && exit

notify-send "$chosen" "copied to clipboard."
c="${chosen%% [A-Z]*}"
echo "$c" | xclip -sel clip
