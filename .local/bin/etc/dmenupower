#!/bin/sh
ANS="$(printf " Lock\n Shutdown\n Reboot\n Exit" | $DMENU -i -p ' System')"
case "$ANS" in
	' Lock') slock & mpc pause;;
	' Shutdown') shutdown now;;
	' Reboot') reboot;;
	' Exit') killall Xorg;;
esac
